# build output
dist/
.astro/

# dependencies
node_modules/

# raw scrape cache (regenerate with the scripts/*.mjs pipeline; not meant to be committed)
scripts/.cache/

# editor/OS
.DS_Store

# Cloudflare/wrangler local state and generated/secret files
.wrangler/
.dev.vars
worker-configuration.d.ts

# local build-time env (see .env.example) — never commit real values
.env
