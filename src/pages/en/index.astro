---
export const prerender = true;
import { readFile } from "node:fs/promises";
import path from "node:path";
import BaseLayout from "../../layouts/BaseLayout.astro";
import Hero from "../../components/Hero.astro";
import localeEquivalents from "../../data/locale-equivalents.json";

const contentHtml = await readFile(
  path.join(process.cwd(), "src/content/main/en/home.html"),
  "utf8",
);
---

<BaseLayout
  title="Rigoni Vittorino"
  description="Rigoni Vittorino: a Veneto wine estate with nearly 100 hectares of vineyards in one of Northern Italy's best wine districts. From the vineyards to the glass, our philosophy."
  ogTitle="Home"
  extraStyleFile="home-extra-style.en.css"
  bodyClass="home"
  lang="en"
  otherLangUrls={localeEquivalents.pages.home}
  showLoadingScreen
>
  <Hero />
  <div class="home-content">
    <Fragment set:html={contentHtml} />
  </div>
</BaseLayout>
